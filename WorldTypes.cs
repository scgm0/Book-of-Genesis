namespace 创世记;

public enum FilterType {
	Linear,
	Nearest
}

public enum EventType {
	Ready,
	Exit,
	Command,
	LeftButtonClick,
	RightButtonClick,
	TextUrlClick
}

public enum TextType {
	All,
	Title,
	LeftText,
	CenterText,
	RightText
}